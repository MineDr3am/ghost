version: "3.1"

networks:
  ghost_network:
services:
  ghost:
    image: ghost:3-alpine
    container_name: ghost
    restart: always
    ports:
      - 2368:2368
    environment:
      url: http://hugolausenazpire.fr

  nginx-proxy:
    image: jwilder/nginx-proxy
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.socks:/tmp/docker.sock:ro
